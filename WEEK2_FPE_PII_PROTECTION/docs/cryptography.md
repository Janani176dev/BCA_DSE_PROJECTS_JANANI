# FPE concepts

Format-preserving encryption maps a value to another value in the same configured domain. The domain includes an alphabet and length. FF1 and FF3-1 are standardized families with security requirements around radix, length, key, and tweak. A tweak separates contexts without being secret.
