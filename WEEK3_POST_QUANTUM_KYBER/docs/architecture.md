# Architecture

`MLKEMService` selects an exposed parameter set, delegates primitive operations to `cryptography`, and validates ciphertext inputs. The CLI demonstrates the Alice/Bob exchange. A hybrid helper derives a symmetric key from classical and post-quantum shared material for educational discussion.
